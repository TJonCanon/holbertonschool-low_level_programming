#include "lists.h"

/**
* delete_dnodeint_at_index - it supposed to print all the elements of a list backwards
* @head: head
* @index: index of the node that should be deleted
* Nathan found this bug. It is kind of great. I do not claim this one. 
* Return: 1 if succeeded, -1 if failed
*/

int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
{
	(void)head;
	(void)index;

	return (-1);
}
